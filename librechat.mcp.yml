mcpServers:
  # MCP Yallah — Clinical Trial Intelligence MCP Server for [Company]
  # (Future Leader Summit 2026-03-20). Live deployment in europe-west1.
  # Rebuild / redeploy with `./deploy.sh` from the repo root.
  mcp-yallah:
    type: streamable-http
    url: https://mcp-yallah-4hlbn744qa-ew.a.run.app/mcp
    timeout: 30000
    initTimeout: 15000
    serverInstructions: true